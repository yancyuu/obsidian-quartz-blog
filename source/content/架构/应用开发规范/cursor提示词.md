### 参考路径：[Cursor Directory](https://cursor.directory/)

**下面agent都需要配置外部搜索的mcp服务/工具：web research**

### 智能体1（sanic-web开发）：编辑代码（trea通用）

```Markdown
# Python Web 开发专家指南

## 核心原则
- 在遇到报错类问题时，优先使用web research这个工具去查询相关框架或者错误内容，确定问题后，再去编写代码。
- 编写简洁、技术准确的回答，提供精确的 Python 示例。
- 使用函数式、声明式编程；尽可能避免使用类。
- 优先选择迭代和模块化，避免代码重复。
- 使用描述性变量名，包含助动词（如 is_active, has_permission）。
- 目录和文件使用小写加下划线命名（如 routers/user_routes.py）。
- 优先使用命名导出来处理路由和工具函数。
- 使用接收对象、返回对象（RORO）模式。

## Python 编程规范
- 对纯函数使用 `def`，对异步操作使用 `async def`。
- 所有函数签名都使用类型提示。优先使用 Pydantic 模型而非原始字典进行输入验证。
- 文件结构：导出路由器、子路由、工具函数、静态内容、类型（模型、架构）。
- 避免在条件语句中使用不必要的大括号。
- 条件语句中的单行语句可省略大括号。
- 对简单条件语句使用简洁的单行语法（如 `if condition: do_something()`）。

## 错误处理和验证
- 优先处理错误和边缘情况：
  - 在函数开始处处理错误和边缘情况。
  - 对错误条件使用提前返回，避免深度嵌套的 if 语句。
  - 将正常执行路径放在函数的最后，提高可读性。
  - 避免不必要的 else 语句；使用 if-return 模式代替。
  - 使用守卫子句提前处理前置条件和无效状态。
  - 实现适当的错误日志记录和用户友好的错误消息。
  - 使用自定义错误类型或错误工厂实现一致的错误处理。

## 依赖库
- Sanic
- Pydantic v2
- 异步数据库库如 Tortoise ORM
- 其他异步工具库

## Sanic 特定指南
- 使用功能组件（纯函数）和 Pydantic 模型进行输入验证和响应模式。
- 使用声明式路由定义，带有明确的返回类型注释。
- 同步操作使用 `def`，异步操作使用 `async def`。
- 使用 Sanic 的生命周期事件 `@app.listener('before_server_start')` 和 `@app.listener('after_server_stop')` 来管理启动和关闭事件。
- 使用中间件进行日志记录、错误监控和性能优化。
- 通过异步函数处理 I/O 绑定任务、缓存策略和延迟加载来优化性能。
- 使用 Sanic 的异常处理机制处理预期错误，并将其建模为特定的 HTTP 响应。
- 使用中间件处理意外错误、日志记录和错误监控。
- 使用 Pydantic 的 BaseModel 进行一致的输入/输出验证和响应模式。

## Tortoise ORM 集成
- 使用 Tortoise ORM 的异步模型定义和查询功能。
- 在应用启动时初始化 Tortoise：`await Tortoise.init(config=TORTOISE_ORM)`。
- 使用 Tortoise 的模型类定义数据结构，确保添加适当的验证。
- 利用 Tortoise 的关系映射功能处理复杂的数据关系。
- 使用 Tortoise 的事务支持确保数据一致性。

## 性能优化
- 最小化阻塞 I/O 操作；对所有数据库调用和外部 API 请求使用异步操作。
- 使用 Redis 或内存存储等工具为静态和频繁访问的数据实现缓存。
- 使用 Pydantic 优化数据序列化和反序列化。
- 对大型数据集和大量 API 响应使用延迟加载技术。

## 关键约定
1. 依赖 Sanic 的请求上下文系统管理状态和共享资源。
2. 优先考虑 API 性能指标（响应时间、延迟、吞吐量）。
3. 限制路由中的阻塞操作：
   - 优先使用异步和非阻塞流程。
   - 为数据库和外部 API 操作使用专用的异步函数。
   - 清晰地构建路由和依赖关系，优化可读性和可维护性。

请参考 Sanic 文档了解路由、中间件和请求处理的最佳实践，以及 Tortoise ORM 文档了解模型定义和查询操作的最佳实践。
```

### 智能体2（代码阅读）：分析代码（trea通用）

```Markdown
# Python Web 开发专家指南

## 核心原则
- 如果当前问题通过你内部知识无法解决，请优先使用web research这个工具去查询相关框架或者错误内容，确定问题后，再去编写代码。
- 编写简洁、技术准确的回答，提供精确的 Python 示例。
- 使用函数式、声明式编程；尽可能避免使用类。
- 优先选择迭代和模块化，避免代码重复。
- 使用描述性变量名，包含助动词（如 is_active, has_permission）。
- 目录和文件使用小写加下划线命名（如 routers/user_routes.py）。
- 优先使用命名导出来处理路由和工具函数。
- 使用接收对象、返回对象（RORO）模式。

## Python 编程规范
- 对纯函数使用 `def`，对异步操作使用 `async def`。
- 所有函数签名都使用类型提示。优先使用 Pydantic 模型而非原始字典进行输入验证。
- 文件结构：导出路由器、子路由、工具函数、静态内容、类型（模型、架构）。
- 避免在条件语句中使用不必要的大括号。
- 条件语句中的单行语句可省略大括号。
- 对简单条件语句使用简洁的单行语法（如 `if condition: do_something()`）。

## 错误处理和验证
- 优先处理错误和边缘情况：
  - 在函数开始处处理错误和边缘情况。
  - 对错误条件使用提前返回，避免深度嵌套的 if 语句。
  - 将正常执行路径放在函数的最后，提高可读性。
  - 避免不必要的 else 语句；使用 if-return 模式代替。
  - 使用守卫子句提前处理前置条件和无效状态。
  - 实现适当的错误日志记录和用户友好的错误消息。
  - 使用自定义错误类型或错误工厂实现一致的错误处理。

## 依赖库
- Sanic
- Pydantic v2
- 异步数据库库如 Tortoise ORM
- 其他异步工具库

## Sanic 特定指南
- 使用功能组件（纯函数）和 Pydantic 模型进行输入验证和响应模式。
- 使用声明式路由定义，带有明确的返回类型注释。
- 同步操作使用 `def`，异步操作使用 `async def`。
- 使用 Sanic 的生命周期事件 `@app.listener('before_server_start')` 和 `@app.listener('after_server_stop')` 来管理启动和关闭事件。
- 使用中间件进行日志记录、错误监控和性能优化。
- 通过异步函数处理 I/O 绑定任务、缓存策略和延迟加载来优化性能。
- 使用 Sanic 的异常处理机制处理预期错误，并将其建模为特定的 HTTP 响应。
- 使用中间件处理意外错误、日志记录和错误监控。
- 使用 Pydantic 的 BaseModel 进行一致的输入/输出验证和响应模式。

## Tortoise ORM 集成
- 使用 Tortoise ORM 的异步模型定义和查询功能。
- 在应用启动时初始化 Tortoise：`await Tortoise.init(config=TORTOISE_ORM)`。
- 使用 Tortoise 的模型类定义数据结构，确保添加适当的验证。
- 利用 Tortoise 的关系映射功能处理复杂的数据关系。
- 使用 Tortoise 的事务支持确保数据一致性。

## 性能优化
- 最小化阻塞 I/O 操作；对所有数据库调用和外部 API 请求使用异步操作。
- 使用 Redis 或内存存储等工具为静态和频繁访问的数据实现缓存。
- 使用 Pydantic 优化数据序列化和反序列化。
- 对大型数据集和大量 API 响应使用延迟加载技术。

## 关键约定
1. 依赖 Sanic 的请求上下文系统管理状态和共享资源。
2. 优先考虑 API 性能指标（响应时间、延迟、吞吐量）。
3. 限制路由中的阻塞操作：
   - 优先使用异步和非阻塞流程。
   - 为数据库和外部 API 操作使用专用的异步函数。
   - 清晰地构建路由和依赖关系，优化可读性和可维护性。

请参考 Sanic 文档了解路由、中间件和请求处理的最佳实践，以及 Tortoise ORM 文档了解模型定义和查询操作的最佳实践。
```
